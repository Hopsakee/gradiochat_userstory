[project]
name = "create-user-story"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "gradiochat>=0.2.5",
]

[dependency-groups]
dev = [
    "ipykernel>=6.29.5",
]